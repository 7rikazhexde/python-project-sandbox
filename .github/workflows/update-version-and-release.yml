name: Version Update and Release

# ワークフローの処理の流れ:
# 1. トリガー条件: 手動実行（workflow_dispatch）でバージョン更新タイプを指定
# 2. pyproject.tomlのバージョンを更新
# 3. コミットとPR作成
# 4. マージ後（再実行時）に同じバージョンでタグとリリースを作成

on:
  workflow_dispatch:
    inputs:
      update_type:
        description: 'Type of version update'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  update-version-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      packages: write

    steps:
      - uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5.3.0
        with:
          python-version: '3.12'

      - name: Install poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry

      - name: Configure Git
        run: |
          git config --local user.email "33836132+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

      - name: Get current version
        id: current_version
        run: echo "version=$(poetry version -s)" >> "$GITHUB_OUTPUT"

      - name: Check if on main branch
        id: check_branch
        run: |
          CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          echo "is_main=$([[ "$CURRENT_BRANCH" == "main" ]] && echo 'true' || echo 'false')" >> "$GITHUB_OUTPUT"

      - name: Check if current version tag exists
        id: check_current_tag
        run: |
          TAG_EXISTS=$(git tag -l "v${{ steps.current_version.outputs.version }}" | wc -l)
          echo "tag_exists=$TAG_EXISTS" >> "$GITHUB_OUTPUT"

      # バージョン更新とPR作成フェーズ
      - name: Update version in pyproject.toml
        id: update_version
        if: steps.check_branch.outputs.is_main == 'true' && steps.check_current_tag.outputs.tag_exists == '0'
        run: |
          echo "Current version before update: $(poetry version -s)"
          poetry version ${{ github.event.inputs.update_type }}
          echo "new_version=$(poetry version -s)" >> "$GITHUB_OUTPUT"

      - name: Create version update PR
        if: steps.check_branch.outputs.is_main == 'true' && steps.check_current_tag.outputs.tag_exists == '0'
        run: |
          git checkout -b "update-version-${{ steps.current_version.outputs.version }}"
          git add pyproject.toml
          git commit -m ":wrench: Bump version to ${{ steps.current_version.outputs.version }}"
          git push -u origin "update-version-${{ steps.current_version.outputs.version }}"

      - name: Create Pull Request
        if: steps.check_branch.outputs.is_main == 'true' && steps.check_current_tag.outputs.tag_exists == '0'
        uses: peter-evans/create-pull-request@v7.0.5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: "update-version-${{ steps.current_version.outputs.version }}"
          title: "Bump version to ${{ steps.current_version.outputs.version }}"
          body: |
            This PR updates the version to ${{ steps.current_version.outputs.version }}.

            After merging this PR:
            1. Wait for all CI checks to pass
            2. Re-run this workflow to create the release
          base: main

      # タグとリリース作成フェーズ
      - name: Generate changelog
        id: changelog
        if: steps.check_branch.outputs.is_main == 'true' && steps.check_current_tag.outputs.tag_exists == '0'
        run: |
          PREV_VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo "")

          if [ -n "$PREV_VERSION" ]; then
            changelog=$(git log --pretty=format:"- %s" "$PREV_VERSION"..HEAD)
          else
            changelog=$(git log --pretty=format:"- %s" -n 10)
          fi

          if [ -z "$changelog" ]; then
            changelog="- Initial release or no significant changes logged"
          fi

          {
            echo "changelog<<EOF"
            echo "$changelog"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create and push tag
        if: steps.check_branch.outputs.is_main == 'true' && steps.check_current_tag.outputs.tag_exists == '0'
        run: |
          git tag "v${{ steps.current_version.outputs.version }}"
          git push origin "v${{ steps.current_version.outputs.version }}"

      - name: Create Release
        if: steps.check_branch.outputs.is_main == 'true' && steps.check_current_tag.outputs.tag_exists == '0'
        uses: softprops/action-gh-release@v2.0.9
        with:
          tag_name: v${{ steps.current_version.outputs.version }}
          name: python-project-sandbox-v${{ steps.current_version.outputs.version }}
          body: |
            ## Changes in this Release

            ${{ steps.changelog.outputs.changelog }}

            For full changes, see the [GitHub commit history](${{ github.server_url }}/${{ github.repository }}/commits/main)
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Output workflow status
        run: |
          echo "Current version: ${{ steps.current_version.outputs.version }}"
          echo "Branch is main: ${{ steps.check_branch.outputs.is_main }}"
          echo "Tag exists: ${{ steps.check_current_tag.outputs.tag_exists }}"
          echo "Current branch: $(git rev-parse --abbrev-ref HEAD)"
